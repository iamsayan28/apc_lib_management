spring.application.name=book-service
spring.datasource.url=jdbc:mysql://localhost:3306/library_db

# Your MySQL username and password
spring.datasource.username=root
spring.datasource.password=sayan@123456789

# --- JPA & Hibernate Settings ---
# This tells Spring Boot to automatically create/update tables based on your @Entity classes.
spring.jpa.hibernate.ddl-auto=update

# This allows you to see the actual SQL queries being run in the console, which is useful for debugging.
spring.jpa.show-sql=true

# --- Server Port (Make sure this is different for each service) ---
# In user-service:
server.port=8081